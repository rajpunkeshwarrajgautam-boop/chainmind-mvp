# Origin of the FastAPI app (used by next.config rewrites for /api/*)
API_ORIGIN=http://127.0.0.1:8000

# Optional: link to OpenAPI in the browser from the shell page
NEXT_PUBLIC_API_ORIGIN=http://127.0.0.1:8000
